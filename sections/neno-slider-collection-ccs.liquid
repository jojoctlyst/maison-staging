        {% render 'neno-slider-collection-ccs', slider_id: section.settings.slider_id, slider_uid: section.id %}

        {% schema %}
            {
  "name": "Collection Card Slider",
  "settings": [
    {
      "type": "select",
      "id": "slider_id",
      "label": "Select a slider",
      "options": [
        {
          "value": "03bceb27-1592-41a3-a0ed-6cfcec071928",
          "label": "Brand Slider"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Collection Card Slider",
      "category": "Slider Collection"
    }
  ]
}
        {% endschema %}